<%= form_with model: @question do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.fields_for :qphotos do |i| %>
    <%= i.file_field :image %>
  <% end %>
  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>